# Git & tooling
.git
.gitignore
.cursor

# Rebuilt inside the Node stage — do not copy host dist
dist

# Node (reinstalled during build)
node_modules

# Logs & env
npm-debug.log*
.env
.env.*
*.local

# Service-account keys — never bake into an image
service-account*.json
*-sa.json
gcp-*.json
